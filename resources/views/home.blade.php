@extends('layouts.app')
@section('title','Dashboard')
@section('content')
<div class="card"><div class="card-content"><div class="row" style="margin-bottom:0"><div class="col s12 m3 center"><img src="{{ $profile['character_icon'] }}" style="width:120px;height:120px;object-fit:cover;border-radius:50%"></div><div class="col s12 m9"><span class="card-title">@if($profile['vip_level']>0)[VIP{{ $profile['vip_level'] }}] @endif{{ $profile['nickname'] }}</span><div class="row"><div class="col s4"><div class="stat">{{ number_format($profile['tr']) }}</div><div class="muted">TR</div></div><div class="col s4"><div class="stat">{{ number_format($profile['cash']) }}</div><div class="muted">Cash</div></div><div class="col s4"><div class="stat">{{ number_format($profile['point']) }}</div><div class="muted">Points</div></div></div>@if($profile['admin'])<a class="btn orange" href="{{ route('admin.index') }}">Administration</a>@endif <a class="btn-flat" href="{{ route('password.form') }}">Change Password</a></div></div></div></div>
<ul class="collapsible"><li><div class="collapsible-header"><i class="material-icons">people</i>Players Online <span class="badge">{{ $online }}</span></div><div class="collapsible-body">@forelse($players as $name)<div class="chip">{{ $name ?: 'Unnamed' }}</div>@empty No players are online. @endforelse</div></li><li><div class="collapsible-header"><i class="material-icons">dns</i>Server Details</div><div class="collapsible-body"><table><tr><td>Server</td><td><span class="{{ $serverOnline ? 'green-text' : 'red-text' }}">{{ $serverOnline ? 'Online' : 'Maintenance / Offline' }}</span></td></tr><tr><td>EXP Rate</td><td>x{{ $settings['MultiplyEXP'] ?? '?' }}</td></tr><tr><td>TR Rate</td><td>x{{ $settings['MultiplyTR'] ?? '?' }}</td></tr><tr><td>Pet EXP Bonus</td><td>{{ $settings['BonusPetExp'] ?? '?' }}</td></tr><tr><td>Double EXP</td><td>{{ !empty($settings['DoubleExp']) ? 'ON' : 'OFF' }}</td></tr><tr><td>Double TR</td><td>{{ !empty($settings['DoubleTR']) ? 'ON' : 'OFF' }}</td></tr><tr><td>Total Members</td><td>{{ number_format($members) }}</td></tr></table></div></li></ul>
@endsection
